# Быстрая инструкция по деплою на Vercel

## 📋 Что нужно сделать:

### 1. Создать репозиторий на GitHub

```bash
# В корне проекта выполните:
git init
git add .
git commit -m "Initial commit"

# Затем на GitHub:
# 1. Перейдите на https://github.com/new
# 2. Создайте новый репозиторий (например, "gameshop")
# 3. НЕ добавляйте README, .gitignore или лицензию
# 4. Скопируйте команды, которые GitHub покажет

# Обычно это:
git remote add origin https://github.com/YOUR_USERNAME/gameshop.git
git branch -M main
git push -u origin main
```

### 2. Деплой на Vercel

1. **Зарегистрируйтесь/войдите на Vercel:**
   - Перейдите на https://vercel.com
   - Нажмите "Sign Up" → "Continue with GitHub"

2. **Импортируйте проект:**
   - Нажмите "Add New..." → "Project"
   - Найдите ваш репозиторий `gameshop`
   - Нажмите "Import"

3. **Настройте переменные окружения:**
   - В разделе "Environment Variables" добавьте:
     ```
     RAWG_API_KEY = ваш_ключ_от_rawg_api
     RAWG_BASE_URL = https://api.rawg.io/api
     ```
   - Выберите все окружения: Production, Preview, Development

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения (2-5 минут)
   - Готово! 🎉

### 3. Проверка

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Каталог игр работает
- ✅ API endpoints отвечают

---

## ⚠️ Важно:

- **Переменные окружения** должны быть добавлены ДО деплоя
- **`.env` файл** НЕ коммитится в Git (уже в .gitignore)
- **In-memory storage** не сохраняется между перезапусками (для продакшена нужна БД)

---

## 📚 Подробная инструкция:

См. `docs/VERCEL_DEPLOYMENT.md` для детальной информации.
