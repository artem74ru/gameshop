# Инструкция по установке и развертыванию проекта Gameshop

Эта инструкция поможет вам установить и запустить проект Gameshop на вашем компьютере.

## 📋 Требования

Перед началом установки убедитесь, что у вас установлены:

- **Node.js** версии 18.x или выше
- **npm** (обычно устанавливается вместе с Node.js)
- **Git** (если вы клонируете проект из репозитория)

### Проверка установки

Откройте терминал (командную строку) и выполните:

```bash
node --version
npm --version
git --version
```

Если команды выводят версии, значит всё установлено правильно.

---

## 🚀 Способы получения проекта

### Вариант 1: Клонирование из GitHub

1. Откройте терминал
2. Перейдите в папку, где вы хотите разместить проект
3. Выполните команду:

```bash
git clone https://github.com/artem74ru/gameshop.git
cd gameshop
```

### Вариант 2: Распаковка архива

1. Распакуйте архив в любую папку
2. Откройте терминал
3. Перейдите в папку проекта:

```bash
cd путь/к/папке/gameshop
```

---

## ⚙️ Установка зависимостей

После того как вы оказались в папке проекта, установите все необходимые зависимости:

```bash
npm install
```

Эта команда может занять несколько минут. Дождитесь завершения установки.

---

## 🔑 Настройка переменных окружения

Проект использует внешние API, для работы которых нужны ключи доступа.

### Шаг 1: Создание файла `.env`

В корне проекта создайте файл с именем `.env` (без расширения).

**Важно:** Файл `.env` должен находиться в корне проекта, рядом с файлом `package.json`.

### Шаг 2: Получение RAWG API ключа

1. Перейдите на сайт [RAWG API](https://rawg.io/apidocs)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел "API" или "My API Key"
4. Создайте новый API ключ (если его нет)
5. Скопируйте полученный ключ

### Шаг 3: Заполнение файла `.env`

Откройте файл `.env` в любом текстовом редакторе и добавьте следующие строки:

```env
RAWG_API_KEY=ваш_ключ_от_rawg_здесь
RAWG_BASE_URL=https://api.rawg.io/api
```

**Пример:**

```env
RAWG_API_KEY=abc123def456ghi789jkl012mno345pqr678
RAWG_BASE_URL=https://api.rawg.io/api
```

**Важно:**
- Не ставьте пробелы вокруг знака `=`
- Не используйте кавычки вокруг значений
- Сохраните файл после редактирования

### Шаг 4: Опциональные настройки

Если вы хотите отключить CheapShark API (для тестирования без цен), добавьте:

```env
DISABLE_CHEAPSHARK=1
```

**Примечание:** CheapShark API не требует ключа, он работает автоматически. Отключайте его только если нужно протестировать проект без функционала сравнения цен.

---

## 🏃 Запуск проекта

### Режим разработки

Для запуска проекта в режиме разработки выполните:

```bash
npm run dev
```

После запуска вы увидите сообщение:

```
✔ Nuxt is ready
  ➜ Local:   http://localhost:3000
```

Откройте браузер и перейдите по адресу **http://localhost:3000**

### Остановка сервера

Для остановки сервера нажмите `Ctrl + C` в терминале.

---

## 🏗️ Сборка для продакшена

Если вы хотите собрать проект для продакшена:

```bash
npm run build
```

После сборки вы можете запустить превью продакшен-версии:

```bash
npm run preview
```

---

## 📁 Структура проекта

```
gameshop/
├── assets/              # Статические ресурсы (изображения, стили)
├── components/          # Vue компоненты
│   ├── Header/          # Компонент шапки сайта
│   ├── Home/            # Компоненты главной страницы
│   └── UIComponents/    # Переиспользуемые UI компоненты
├── layouts/             # Макеты страниц
├── pages/               # Страницы приложения
│   ├── game/            # Страницы игр
│   ├── catalog.vue      # Каталог игр
│   ├── cart.vue         # Корзина
│   └── index.vue        # Главная страница
├── server/              # Серверная часть (API endpoints)
│   ├── api/             # API маршруты
│   └── utils/           # Утилиты для работы с API
├── stores/              # Pinia хранилища (состояние приложения)
├── public/              # Публичные файлы
├── .env                 # Переменные окружения (создайте сами)
├── nuxt.config.ts       # Конфигурация Nuxt
├── package.json         # Зависимости проекта
└── tsconfig.json        # Конфигурация TypeScript
```

---

## 🔧 Решение проблем

### Проблема: "RAWG API не настроен"

**Решение:** Проверьте файл `.env`:
- Убедитесь, что файл находится в корне проекта
- Проверьте, что переменные написаны правильно (без пробелов вокруг `=`)
- Убедитесь, что вы указали правильный API ключ

### Проблема: "Cannot find module" или ошибки импорта

**Решение:** Переустановите зависимости:

```bash
rm -rf node_modules package-lock.json
npm install
```

**Для Windows:**

```bash
rmdir /s node_modules
del package-lock.json
npm install
```

### Проблема: Порт 3000 уже занят

**Решение:** Nuxt автоматически попробует использовать следующий доступный порт (3001, 3002 и т.д.). Или вы можете указать другой порт:

```bash
npm run dev -- --port 3001
```

### Проблема: Игры не загружаются

**Решение:**
1. Проверьте, что файл `.env` настроен правильно
2. Убедитесь, что RAWG API ключ действителен
3. Проверьте консоль браузера (F12) на наличие ошибок
4. Проверьте терминал на наличие ошибок сервера

### Проблема: Ошибки TypeScript

**Решение:** Убедитесь, что все зависимости установлены:

```bash
npm install
```

Если проблема сохраняется, попробуйте:

```bash
npm run build
```

Это покажет все ошибки TypeScript, если они есть.

---

## 🌐 Развертывание на Vercel

Если вы хотите развернуть проект на Vercel:

1. Зарегистрируйтесь на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. В настройках проекта добавьте переменные окружения:
   - `RAWG_API_KEY` = ваш ключ от RAWG
   - `RAWG_BASE_URL` = `https://api.rawg.io/api`
4. Нажмите "Deploy"

Подробная инструкция по развертыванию на Vercel находится в файле `docs/VERCEL_DEPLOYMENT.md`

---

## 📚 Дополнительная документация

- `README.md` - Основная информация о проекте
- `docs/VERCEL_DEPLOYMENT.md` - Инструкция по развертыванию на Vercel
- `docs/VERCEL_ENV_VARIABLES.md` - Настройка переменных окружения на Vercel
- `docs/CHEAPSHARK_RATE_LIMITING.md` - Информация о работе с CheapShark API

---

## ✅ Проверка работоспособности

После установки проверьте:

1. ✅ Проект запускается без ошибок (`npm run dev`)
2. ✅ Главная страница открывается (http://localhost:3000)
3. ✅ Каталог игр загружается (`/catalog`)
4. ✅ Страница игры открывается (например, `/game/grand-theft-auto-v`)
5. ✅ Корзина работает (`/cart`)

---

## 🆘 Получение помощи

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте терминал на наличие ошибок сервера
4. Убедитесь, что все требования выполнены
5. Убедитесь, что файл `.env` настроен правильно

---

## 📝 Примечания

- **Не коммитьте файл `.env` в Git!** Он уже добавлен в `.gitignore`
- CheapShark API не требует ключа, но имеет ограничения по количеству запросов
- Проект использует кэширование для оптимизации запросов к API
- Все API запросы выполняются на сервере (безопасность)

---

**Удачной работы с проектом! 🎮**
