# Деплой проекта на Vercel

## Шаг 1: Подготовка проекта

### 1.1. Проверка .gitignore

Убедитесь, что `.gitignore` содержит:
- `node_modules/`
- `.env` (важно! не коммитим секретные ключи)
- `.nuxt/`
- `.output/`
- `dist/`

### 1.2. Проверка package.json

Убедитесь, что в `package.json` есть:
- `"build": "nuxt build"` - команда для сборки
- Все необходимые зависимости указаны

---

## Шаг 2: Создание репозитория на GitHub

### 2.1. Инициализация Git (если еще не сделано)

```bash
# В корне проекта
git init
```

### 2.2. Добавление всех файлов

```bash
git add .
```

### 2.3. Первый коммит

```bash
git commit -m "Initial commit: Gameshop project"
```

### 2.4. Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Заполните:
   - **Repository name**: `gameshop` (или другое название)
   - **Description**: "Game shop application built with Nuxt 3"
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
3. Нажмите "Create repository"

### 2.5. Подключение локального репозитория к GitHub

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/gameshop.git
git branch -M main
git push -u origin main
```

---

## Шаг 3: Деплой на Vercel

### 3.1. Регистрация/Вход на Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Sign Up" или "Log In"
3. Выберите "Continue with GitHub" для авторизации через GitHub

### 3.2. Импорт проекта

1. На главной странице Vercel нажмите "Add New..." → "Project"
2. Найдите ваш репозиторий `gameshop` в списке
3. Нажмите "Import"

### 3.3. Настройка проекта

Vercel автоматически определит Nuxt 3, но проверьте настройки:

**Framework Preset:** Nuxt.js (должно определиться автоматически)

**Root Directory:** `./` (оставьте как есть)

**Build Command:** `npm run build` (должно быть автоматически)

**Output Directory:** `.output` (должно быть автоматически)

**Install Command:** `npm install` (должно быть автоматически)

### 3.4. Настройка переменных окружения

**ВАЖНО:** Добавьте переменные окружения перед деплоем!

В разделе "Environment Variables" добавьте:

```
RAWG_API_KEY = ваш_ключ_от_rawg_api
RAWG_BASE_URL = https://api.rawg.io/api
```

**Как добавить:**
1. В настройках проекта найдите "Environment Variables"
2. Добавьте каждую переменную:
   - **Name**: `RAWG_API_KEY`
   - **Value**: ваш ключ API
   - **Environment**: Production, Preview, Development (выберите все три)
3. Повторите для `RAWG_BASE_URL`

### 3.5. Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя вы получите URL вида: `https://gameshop-xxx.vercel.app`

---

## Шаг 4: Проверка деплоя

### 4.1. Проверка работы сайта

1. Откройте URL вашего проекта
2. Проверьте, что главная страница загружается
3. Проверьте каталог игр
4. Проверьте страницу игры

### 4.2. Проверка API

Проверьте, что API endpoints работают:
- `/api/games`
- `/api/genres`
- `/api/platforms`

---

## Шаг 5: Настройка домена (опционально)

### 5.1. Добавление кастомного домена

1. В настройках проекта перейдите в "Domains"
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS

---

## Важные замечания

### ✅ Что работает на Vercel:

- ✅ SSR (Server-Side Rendering)
- ✅ API Routes (`/server/api/*`)
- ✅ Server Middleware
- ✅ Static Generation
- ✅ Environment Variables

### ⚠️ Ограничения:

- **In-memory storage** (как в `server/utils/db.ts`) **НЕ сохраняется** между перезапусками
  - Для продакшена нужна внешняя БД (PostgreSQL, MongoDB и т.д.)
- **Кеш в памяти** (как в API endpoints) **НЕ сохраняется** между деплоями
  - Vercel использует serverless функции, которые могут перезапускаться
  - Для постоянного кеша используйте Redis или внешнее хранилище

### 🔒 Безопасность:

- ✅ Переменные окружения хранятся безопасно на Vercel
- ✅ `.env` файл не коммитится в Git
- ✅ HTTPS включен автоматически

---

## Автоматический деплой

После настройки:
- Каждый push в `main` ветку автоматически деплоит на Production
- Pull Requests автоматически создают Preview деплои
- Можно настроить разные окружения для разных веток

---

## Troubleshooting

### Ошибка: "Module not found"
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что `node_modules` не коммитятся

### Ошибка: "Environment variable not found"
- Проверьте, что переменные окружения добавлены в Vercel
- Убедитесь, что выбраны правильные окружения (Production, Preview, Development)

### Ошибка: "Build failed"
- Проверьте логи сборки в Vercel
- Убедитесь, что `npm run build` работает локально
- Проверьте версии Node.js (Vercel использует Node.js 18.x по умолчанию)

### API не работает
- Проверьте, что переменные окружения добавлены
- Проверьте логи функций в Vercel Dashboard
- Убедитесь, что API endpoints доступны (не защищены middleware)

---

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Nuxt 3 Deployment](https://nuxt.com/docs/getting-started/deployment)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
